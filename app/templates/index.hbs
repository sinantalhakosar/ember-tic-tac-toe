<div
  class="mb-0 xl:px-0 mt-8 flex flex-col items-center justify-between w-full flex-1"
>
  <div class="max-w-2xl mb-8 flex flex-col gap-10 justify-center h-full">
    <div class="flex flex-wrap items-center justify-center gap-10">
      <button
        class="text-white bg-blue-600 hover:bg-blue-700 focus:ring-4 focus:ring-blue-800 font-large rounded-lg text-lg px-5 py-2.5 me-2 mb-2 focus:outline-none"
        data-testid="play-with-ai-button"
        type="button"
        {{on "click" this.playGameWithAI}}
      >
        Play Against AI
      </button>

      <button
        class="text-white bg-red-600 hover:bg-red-700 focus:ring-4 focus:ring-red-800 font-large rounded-lg text-lg px-5 py-2.5 me-2 mb-2 focus:outline-none"
        data-testid="play-button"
        type="button"
        {{on "click" this.playGame}}
      >
        Play
      </button>

      <div class="px-2 me-2 mb-2 flex items-center justify-center gap-2">
        <span
          class="gap-2 py-2 px-3 text-xl text-white rounded"
          data-testid="label"
        >
          First player is:
        </span>

        <button
          class="w-20 h-10 rounded-full bg-slate-300 flex items-center"
          type="button"
          {{on "click" this.handleIconClick}}
        >
          <div
            class="flex items-center justify-center w-12 h-12 relative rounded-full transition duration-300
              {{if
                (eq this.game.startingPlayer 'X')
                'bg-red-500 -translate-x-2'
                'bg-blue-500 translate-x-full'
              }}"
          >
            {{#if (eq this.game.startingPlayer 'X')}}
              <IconifyIcon
                @icon="ri:close-line"
                width="32"
                height="32"
                color="#fff"
              />
            {{else}}
              <IconifyIcon
                @icon="ri:circle-line"
                width="32"
                height="32"
                color="#fff"
              />
            {{/if}}
          </div>
        </button>
      </div>
    </div>

    <div class="flex items-center justify-center w-8/12 2xl:w-full self-center" data-testid="logo">
      <IconifyIcon
        @icon="tabler:tic-tac"
        width="100%"
        height="100%"
        color="white"
      />
    </div>

    <p
      class="p-5 text-md xl:text-xl leading-normal lg:text-xl 2xl:text-2xl text-gray-300 text-center"
      data-testid="description"
    >
      Tic-tac-toe, or Xs and Os is a game for two players
    </p>

  </div>
  <div class="bg-transparent rounded-lg shadow bg-transparent w-full sm:w-9/12 pb-4">
    <hr class="my-6 sm:mx-auto border-gray-700" />
    <span class="block text-sm text-center text-gray-400">© 2024 Sinan Talha
      KOSAR. All Rights Reserved.</span>
  </div>
</div>